general:
  max_timesteps: 50
model:
  include:
    path: "../common/model_defaults.yaml"
    override: False
  segment:
    initial_damage_distribution: [0.55, 0.15, 0.15, 0.15 ,0]
  network:
    travel_time_reward_factor: -50000.0
network:
  include:
    path: "./network.yaml"
